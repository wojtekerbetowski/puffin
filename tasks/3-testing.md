* Improve the hashing test
  * Add more test cases (e.g. empty file)
  * Create the temporary working directory
  * Prepare the test files (write the proper content)
  * Use `pytest` fixture to simplify the test
